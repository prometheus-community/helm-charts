{{- if .Values.prometheus.enabled -}}
  {{- $serviceName := printf "%s-%s" (include "kube-prometheus-stack.fullname" .) "thanos-discovery" -}}
  {{- $servicePort := .Values.prometheus.thanosIngress.servicePort | default .Values.prometheus.thanosService.port -}}
  {{- range $name, $route := .Values.prometheus.thanosRoute }}
  {{- if $route.enabled }}
---
{{ include "kube-prometheus-stack.route" (dict
     "context" $
     "route" $route
     "name" (ternary $serviceName (printf "%s-%s" $serviceName $name) (eq $name "main"))
     "namespace" (include "kube-prometheus-stack.namespace" $)
     "labelApp" (printf "%s-thanos-discovery" (include "kube-prometheus-stack.name" $))
     "hostnames" $route.hostnames
     "serviceName" $serviceName
     "servicePort" $servicePort
   ) }}
  {{- end }}
  {{- end }}
{{- end }}
