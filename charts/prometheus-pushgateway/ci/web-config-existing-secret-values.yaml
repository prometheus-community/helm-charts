webConfiguration:
  existingSecret:
    name: pushgateway-web-config

extraManifests:
  - apiVersion: v1
    kind: Secret
    metadata:
      name: pushgateway-web-config
      namespace: '{{ include "prometheus-pushgateway.namespace" . }}'
    type: Opaque
    stringData:
      web-config.yaml: |
        {}
