---
config:
  postgresExporter: |-
    auth_modules:
      shared:
        type: userpass
        userpass:
          username: postgres-exporter
          password: firstpass
        options:
          sslmode: disable
          dbname: postgres
