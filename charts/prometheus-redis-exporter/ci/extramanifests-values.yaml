extraManifests:
- apiVersion: external-secrets.io/v1beta1
  kind: SecretStore
  metadata:
    name: '{{ include "prometheus-redis-exporter.fullname" . }}'
  spec:
    provider:
      vault:
        auth:
          kubernetes:
            mountPath: kubernetes
            role: prometheus-redis-exporter
            serviceAccountRef:
              name: '{{  include "prometheus-redis-exporter.serviceAccountName" .  }}'
        path: secret
        server: https://vault.server
        version: v2
