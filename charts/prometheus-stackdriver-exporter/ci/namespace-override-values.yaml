namespaceOverride: default

serviceMonitor:
  enabled: true
  namespace: default

prometheusRule:
  enabled: true
  namespace: default
  rules:
    - alert: StackdriverExporterHeartbeat
      expr: vector(1)
      for: 1m
      labels:
        severity: none
      annotations:
        summary: Stackdriver exporter heartbeat
