commonLabels:
  team: observability
  owner: platform

serviceMonitor:
  enabled: true

prometheusRule:
  enabled: true
  rules:
    - alert: CloudwatchExporterSample
      expr: up == 0
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: sample
