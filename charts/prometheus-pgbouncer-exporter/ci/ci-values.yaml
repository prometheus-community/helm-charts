rbac:
  create: true
  pspEnabled: false
extraContainers:
  - name: pgbouncer
    image: edoburu/pgbouncer:latest
    env:
      - name: DB_HOST
        value: "postgres-host"
config:
  datasource:
    host: localhost
    port: 5432
    password: "password"
